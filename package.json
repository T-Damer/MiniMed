{
  "name": "localmed-search",
  "version": "0.3.0-alpha.6",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter @localmed/app dev",
    "dev:landing": "pnpm --filter @localmed/landing dev",
    "clean": "node scripts/clean.mjs",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check .",
    "check:write": "biome check --write .",
    "typecheck": "pnpm -r --if-present typecheck",
    "test:unit": "vitest run",
    "test:python": "uv run --project tools/ingest pytest tools/ingest/tests",
    "test": "pnpm test:unit && pnpm test:python",
    "test:watch": "vitest",
    "test:e2e": "pnpm build:app && playwright test",
    "schema:generate": "node scripts/generate-schema.mjs",
    "schema:check": "node scripts/generate-schema.mjs --check",
    "content:sync": "uv sync --project tools/ingest --all-groups",
    "content:lint": "uv run --project tools/ingest medbase lint --input content/fixtures",
    "content:build": "pnpm content:compile && pnpm content:publish",
    "content:lint:pilot": "uv run --project tools/ingest medbase lint --input content/pilot-rf",
    "content:build:pilot": "uv run --project tools/ingest medbase build --input content/pilot-rf --output data/build/rf-public-pilot.db --report data/build/rf-public-pilot-report.json",
    "content:prepare:private": "uv run --project tools/ingest medbase prepare --registry data/raw/sources.yaml --source-root data/raw --output data/intermediate/private-pilot --force",
    "content:lint:private": "uv run --project tools/ingest medbase lint --input data/intermediate/private-pilot",
    "content:build:private": "uv run --project tools/ingest medbase build --input data/intermediate/private-pilot --output data/build/private-pilot.db --report data/build/private-pilot-report.json",
    "python:check": "uv run --project tools/ingest ruff format --check tools/ingest/src tools/ingest/tests && uv run --project tools/ingest ruff check tools/ingest/src tools/ingest/tests && uv run --project tools/ingest pyright tools/ingest/src tools/ingest/tests && uv run --project tools/ingest pytest tools/ingest/tests",
    "benchmark:search": "pnpm --filter @localmed/benchmarks benchmark:search",
    "benchmark:pilot": "pnpm --filter @localmed/benchmarks benchmark:pilot",
    "peers:check": "pnpm peers check",
    "build:app": "pnpm --filter @localmed/app build",
    "build:landing": "pnpm --filter @localmed/landing build",
    "build": "pnpm schema:check && pnpm build:app && pnpm build:landing",
    "native:add:android": "pnpm --filter @localmed/app cap:add:android",
    "native:add:ios": "pnpm --filter @localmed/app cap:add:ios",
    "native:sync": "pnpm --filter @localmed/app cap:sync",
    "native:sync:android": "pnpm --filter @localmed/app cap:sync:android",
    "native:sync:ios": "pnpm --filter @localmed/app cap:sync:ios",
    "verify": "node scripts/verify.mjs",
    "verify:all": "node scripts/verify.mjs && pnpm test:e2e",
    "secrets:check": "node scripts/no-secrets.mjs",
    "content:test": "pnpm test:python",
    "content:compile": "uv run --project tools/ingest medbase build --input content/fixtures --output data/build/core-demo.db --json-output packages/test-fixtures/src/generated/core-demo.json --report data/build/core-demo-report.json",
    "content:publish": "node scripts/publish-demo-pack.mjs",
    "benchmark:cases": "pnpm --filter @localmed/benchmarks benchmark:cases",
    "benchmark:all": "pnpm benchmark:search && pnpm benchmark:cases",
    "native:source:check": "node scripts/check-native-bridge.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "@playwright/test": "1.61.1",
    "@types/node": "26.1.1",
    "tsx": "4.23.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tools/benchmarks"
  ],
  "overrides": {
    "vite": "8.1.4",
    "astro": "7.0.9"
  }
}
