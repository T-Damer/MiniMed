# Content-first execution override — 22 September 2026

The user explicitly changed the priority: collect terms and update the databases. MiniMed is in
DEV; code and prepared file formats may change without preserving backward compatibility. Do not
spend this iteration adding compatibility adapters, streaming installers or speculative compression.
Existing personal data and private sources must still never be deleted, overwritten or published.
This instruction changes execution order, not the source-fidelity, provenance or rights boundaries.

## Next implemented slice

1. Add a separate mass-collected Russian terminology source family, rather than re-counting the
   previous 18,133 records. Keep original wording, supplied etymology and source-specific meanings.
2. Preserve distinct page/source identities and exact retrieval receipts. Report newly covered names,
   additional descriptions of known names, rejected/empty candidates and incomplete source coverage
   separately. Encyclopedic descriptions of scales are not executable scoring implementations.
3. Rebuild the actual local SQLite reference from a verified input manifest. Remove the fixed
   18,133-entry assumption from the preparer; derive expected counts from checksummed source inputs.
4. Update the active content manifest, plan and CURRENT_STATE with measured collection/build results.
   No invented counts, new model dependency, merge, public data release or APK publication.

The first new candidate family is Russian Wikipedia medical/anatomical/diagnostic introductions.
Community material remains `requires-review`, with attribution and source links. This is broader
source coverage, not clinician approval. API snapshots must not be represented as a revision-pinned
quote unless the response itself proves the revision binding. Existing clinical-source excerpts and
licensed Russian Wiktionary senses remain separate source families.

The owner-supplied Semenov/Bersenev textbook keeps its 2006 wording and its existing private module.
Its 450 previously extracted records are not new public records in this batch. No owner-derived text
is sent to GitHub or public CI. Full-scale forms, cutoffs and person/discovery links are admitted only
when source evidence supplies them; a title or a surname alone is not enough.

This dated override takes precedence over the older R1b/R2 execution-priority paragraphs in
DEFINITION_REFERENCE_PLAN.md. Open device/lifecycle gates remain recorded, but are not the work
focus until content collection and the new edition have been delivered.
