# OCR work track (Qwen / OpenRouter)

**Agent:** MiniMed content agent
**Scope:** Neonatology (Шабалов, неонатология, 2016)
**Model:** `qwen/qwen2.5-vl-72b-instruct` (OpenRouter chat completions vision endpoint)
**Run source:** `/tmp/minimed-ocr-qwen-neon/`

## 16 августа 2026 (continuation)

- Выполнены/дополнены целевые проходы по страницам, которые раньше попадали частично в `qwen-run.json`.
- Основной пакет: `qwen/qwen2.5-vl-72b-instruct`.
- На нестабильный хвостовой API-канал были сделаны 2 попытки большого батча: обе сворачивались на SSL/TLS read/hang, поэтому выполнен минимальный стабилизирующий прогон `qwen/qwen3-vl-8b-instruct` (1 страница) для проверки доступности.
- Проверка статуса: все завершившиеся ответы `status: ok`, `parseError: null`, без потери разметки.

### Cost / throughput

- Итог по этим прогонам на сегодня:
  - `qwen/qwen2.5-vl-72b-instruct`: **$0.211393** (81 страница)
  - `qwen/qwen3-vl-8b-instruct`: **$0.0008013** (1 страница, 390)
  - **Total this pass:** **$0.2121943**
- Предыдущая заявленная сессия с Qwen: ~**$0.013797** (14 стр.) — см. старые строки этого же файла.

### Покрытые страницы (уникально, по завершившимся ответам)

- **367–390** (полный набор страниц из двух ранних чанков, включая 369, 372, 374–379, 385, 386)
- **394, 405, 410**
- **451–503**
- **677**

### Остатки/пробелы для неонатологии (для ручной проверки в `LITERATURE_REVIEW_QUEUE`)

- До сих пор НЕ закрыты: `391, 395–404, 406–409, 411–450, 504–510`.
- Эти диапазоны сейчас проходят OCR и остаются НЕ hand-verified для машинно-валидируемых значений целевых калькуляторов.

### 16 августа 2026 (targeted re-run for missing ranges)

- Запущен последовательный таргетный проход `qwen2.5` для всех ранее пропущенных диапазонов.
- Модель и параметр: `qwen/qwen2.5-vl-72b-instruct`, единичный per-page режим (чтобы избежать сетевых зависаний на батчах).
- Дополнительный охват:
  - `391`
  - `395–404`
  - `406–409`
  - `411–450`
  - `504–510`
- Стоимость этого прохода: **$0.2736052** по `usage.cost` из API ответов.
- Итоговое покрытие по этим страницам: **62 страницы**.
- Обновлённые выводы:
  - По этим 62 страницам все ответы `status: ok`, `parseError: null`, без зависаний.
  - `data.page` в ответах смещён на +1 относительно запрошенного номера на большинстве файлов (например `391` → `data.page=392`) — важный артефакт извлечения, требующий ручной сверки `headings/tables`.
  - `LITERATURE_REVIEW_QUEUE` остаётся открытым до `hand-verified`; OCR-след уже создан.

### Итоговые остатки (непроверенные вручную для публикации)

- Для неонатологии по данным страницам сейчас закрыт список `391, 395–404, 406–409, 411–450, 504–510` как "прогнанный OCR".
- "Машинно-валидированная готовность для шиптров/инструментов" по этим страницам остаётся на ручной проверке.

### Результат по содержимому

- Извлечено много таблиц (см. `qwen2.5-all-summary.ndjson`), но **ключевые blockers по `LITERATURE_REVIEW_QUEUE` по-прежнему открыты**:
  - Fenton/INTERGROWTH: только ссылочный упор на Табл/рис. 8.x без пригодного численного массива;
  - bilirubin/phototherapy пороги по часу жизни: описания есть, но матрицы нет;
  - неонатальные лабораторные референсы: единичные фрагменты без полноценного age/param block.

### Следующие шаги

- Зафиксировать в `docs/LITERATURE_REVIEW_QUEUE.md`, какие из `requiresOCR` страниц можно закрывать после ручной проверки и какие блоки останутся ожидающими.
- Если сеть снова стабилизируется, дооценить диапазон `391–510` и закрывать только после `hand-verified` проверки каждой матрицы.
